package fr.erazor.weatherpal.viewmodel

class CardViewModel {
    fun createCards() {
        // Create 6 cards with the same layout but not the same id
        // that all will belong to the LinearLayout that has the id "scrollCards"
    }
}