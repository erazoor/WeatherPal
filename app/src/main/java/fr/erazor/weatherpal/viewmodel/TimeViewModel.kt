package fr.erazor.weatherpal.viewmodel

class TimeViewModel {
    // Create a function that gets the current time of the phone
    // And based on the time the background it will return an integer value
    // If the time is between 9:00 and 17:00 the value will be 1
    // If the time is between 17:00 and 21:00 the value will be 2
    // If the time is between 21:00 and 9:00 the value will be 3
}